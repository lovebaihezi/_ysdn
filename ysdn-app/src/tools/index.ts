import { Check } from './permission';
import * as rules from './permission/rules';
import * as handleError from './handleError';
import { tips, formTake, valid } from './account';

export { Check, rules, handleError, tips, formTake, valid };
