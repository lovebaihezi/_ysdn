import { Result } from 'antd';
import { FC } from 'react';
const WebConnectionResult: <T, E>() => FC = () => props => {
    console.log(props);
    return <></>;
};
export default WebConnectionResult;
