import { createUser } from './createInitJson';

describe('create init json test', () => {
    test('test create success?', async () => {
        // let index = 0;
        // for await (const i of createUser()) {
        //     if (index > 1) {
        //         break;
        //     }
        //     index += 1;
        //     expect(i).toBeDefined();
        // }
    });
});
