import { Col, Row } from 'antd';
import React from 'react';

export default function UserLink({ link }: { link: string; avatr: string }) {
    return (
        <Row wrap={false}>
            <Col></Col>
        </Row>
    );
}
